-- Historical no-op stub.
-- This version exists in remote supabase_migrations (applied as a "rollback"
-- file that was incorrectly placed under migrations/). Kept so local/remote
-- lists match. Workspaces are superseded by firms / firm_members.

select 1;
