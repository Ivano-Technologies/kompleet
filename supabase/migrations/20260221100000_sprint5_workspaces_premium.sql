-- Historical no-op stub.
-- Originally created workspaces / workspace_members (Sprint 5). That surface was
-- superseded by firms / firm_members (Phase 3 tenancy spine). The migration was
-- already applied on the remote project; this file is retained so
-- `supabase migration list` stays in sync. Do not re-introduce workspace tables.

select 1;
