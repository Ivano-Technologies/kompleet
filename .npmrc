# Use legacy peer deps to avoid ESLint / Next conflicts
legacy-peer-deps=true

# Faster + more reliable installs
fund=false
audit=false

# Prevent Windows path / symlink issues
prefer-offline=true

# Consistent dependency resolution
engine-strict=false
