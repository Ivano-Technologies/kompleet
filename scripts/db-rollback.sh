#!/bin/bash
# Retired with the supabase/ tree (IVA-64 Phase 5).
echo "db-rollback.sh: retired — Postgres rollbacks are no longer in-repo."
echo "Phase 6 (IVA-65) will pause/delete the hosted Supabase project after CoS verifies the strip."
exit 1
